# V27 — Checkout تا پرداخت

جریان Backend:
1. مشتری با session معتبر وارد checkout می‌شود.
2. آدرس متعلق به همان مشتری بررسی می‌شود.
3. قیمت و موجودی از DB خوانده می‌شود.
4. سفارش با وضعیت pending_payment ساخته می‌شود.
5. API ایجاد پرداخت فراخوانی می‌شود.
6. درگاه به callback برمی‌گردد.
7. verify سمت سرور انجام می‌شود.
8. فقط پس از verify موفق، سفارش paid/processing می‌شود.
9. موجودی در transaction کم می‌شود.

برای جلوگیری از خطا، مبلغ پرداخت هرگز از frontend پذیرفته نمی‌شود.
